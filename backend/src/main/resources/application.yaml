server:
  port: 8080
  servlet:
    context-path: /auction-web


spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/auction_web"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
